"""Library — data inventory + upload + record edit. Stub; fleshed out next."""

import streamlit as st

st.title("Library")
st.markdown(
    '<p class="page-lede">Every report ingested, its freshness, and the '
    'records it produced.</p>',
    unsafe_allow_html=True,
)
st.info("This page lands in commit 3 (the next one).")
